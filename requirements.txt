# Core dependencies
playwright==1.44.0
beautifulsoup4==4.12.3
requests==2.32.3
PyYAML==6.0.1

# Logging (optional, built-in but version tracking if needed)
# logging is built-in to Python

# Optional for captcha, email, or SMS APIs if used:
# 2captcha-python
# cloudscraper

# If you're using mail.tm API or similar, maybe:
# aiohttp==3.9.5

# If using dotenv files for API keys:
# python-dotenv==1.0.1